import { PlaceholderScreen } from '@/components/ui/PlaceholderScreen';

export default function AboutScreen() {
  return (
    <PlaceholderScreen
      title="Über Trazia"
      subtitle="Version, Credits und Lizenzen — kommt in CC-3.10."
    />
  );
}
