import { PlaceholderScreen } from '@/components/ui/PlaceholderScreen';

export default function BackupScreen() {
  return (
    <PlaceholderScreen title="Backup" subtitle="iCloud / Google-Drive Backup — kommt in CC-3.10." />
  );
}
