import { PlaceholderScreen } from '@/components/ui/PlaceholderScreen';

export default function ExportScreen() {
  return (
    <PlaceholderScreen
      title="Daten-Export"
      subtitle="JSON / CSV-Export deiner Reisen — kommt in CC-3.10."
    />
  );
}
