import { PlaceholderScreen } from '@/components/ui/PlaceholderScreen';

export default function PrivacyScreen() {
  return (
    <PlaceholderScreen
      title="Datenschutz"
      subtitle="Lokale Datenverarbeitung, keine Server — Details in CC-3.10."
    />
  );
}
